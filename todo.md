# TODO

- view each agent's output/error via drawer, not just indeterminate state
- include files in new issue prompt
- difference between issue title and issue description/prompt?
- download implementer output
- project should be configurable not as issue input
- users should be scoped to project
- users should be included via email invite
- agents should be configurable per project
- agent should be selectable via new issue if there are multiple choices
